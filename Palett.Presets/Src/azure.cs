using Palett.Types; 
using static Palett.Cards;

namespace Palett {
  public static partial class Presets {
    public static Preset Azure = new Preset {
      Max = Cyan.Accent1,
      Min = LightBlue.Accent4,
      Na = DeepOrange.Accent1,
    };
  }
}