using Palett.Types; 
using static Palett.Cards;

namespace Palett {
  public static partial class Presets {
    public static Preset Aurora = new Preset {
      Max = Green.Accent3,
      Min = DeepPurple.Accent1,
      Na = Teal.Accent1,
    };
  }
}